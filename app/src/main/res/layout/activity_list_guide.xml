<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_list_guide"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/bg_common"
    tools:context="com.hengda.smart.xhnyw.d.ui.ListGuideActivity">

    <include
        android:id="@+id/header_list"
        layout="@layout/layout_head" />

    <com.yarolegovich.discretescrollview.DiscreteScrollView
        android:id="@+id/transpager_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/header_list"
        android:layout_marginBottom="35dp" />
    <Button
        android:id="@+id/btn_enter_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="5dp"
        android:text="@string/enter_exhibition"
        android:background="@drawable/select_start"
        android:textColor="@color/colorWhite"
        android:textSize="18sp" />
</RelativeLayout>
